name: Example
on: [push]

jobs:
  echo_job:
    runs-on: ubuntu-latest

    steps:
      - name: Mask fruit
        uses: hritesh93/my-action@main
        with:
          fruit: banana

      - name: Echo fruit
        run: echo "apple, banana, and orange"
